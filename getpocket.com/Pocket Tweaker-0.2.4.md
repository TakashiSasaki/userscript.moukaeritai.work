# Pocket Tweaker

## スクリプト概要

このTampermonkeyユーザースクリプトは、Pocketウェブサイト（`https://getpocket.com/my-list`、`https://getpocket.com/read/*`）の読書体験を向上させることを目的としています。具体的には、`<pre>`タグの背景色が暗すぎる問題と、`<article>`タグの表示領域が狭すぎるという二つの問題を解決します。

## 主な機能と動作

-   **記事コンテンツの幅の拡大**: `<article>`要素の`maxWidth`を自動的に「96%」に調整し、記事のメインコンテンツをより広く表示することで、画面スペースを有効活用し、読みやすさを向上させます。
-   **コードブロックのスタイリング強化**: 記事内の`<pre>`タグを再スタイリングし、特にコードブロックの見た目と読みやすさを改善します。具体的な調整内容は以下の通りです。
    -   `background`を「initial」にリセットし、暗すぎる背景色を防ぎます。
    -   `borderColor`を`--(color-textSecondary)`（Pocket独自のCSS変数）に設定し、`borderStyle`を「dashed」にします。
    -   `margin`を「initial」にリセットします。
    -   `width`を「85vw」（ビューポート幅の85%）に設定し、`<pre>`ブロックが適切な幅を持つようにします。
-   **動的なコンテンツ監視**: メインコンテンツエリア（`BODY > DIV`）の変更を監視するために`MutationObserver`を利用します。これにより、記事がロードされたり、コンテンツが変更されたりした場合（例: 読書モードでの表示）、スタイリングの調整が動的に適用されることを保証します。

## スクリプトの目的

このスクリプトは、Pocketでコンテンツ、特にコードスニペットを含む記事を閲覧するユーザーに対して、より快適で視覚的に魅力的な読書環境を提供することを目指しています。

## 技術詳細

-   **MutationObserver**: ページのDOMツリーの変更を監視し、記事コンテンツの動的な更新を検知してスタイルを適用します。
-   **DOM操作**: `document.querySelector`や要素の`style`プロパティを直接操作して、CSSスタイルを適用します。

## 使用方法

1.  Tampermonkeyまたは同様のユーザースクリプトマネージャーをブラウザにインストールします。
2.  このスクリプトをインストールします。
3.  Pocketのウェブサイト（`https://getpocket.com/my-list`または`https://getpocket.com/read/*`に一致するURL）にアクセスします。
4.  記事ページでは、メインの記事コンテンツがより広い幅で表示され、`<pre>`タグのコードブロックが改善されたスタイルで表示されます。
