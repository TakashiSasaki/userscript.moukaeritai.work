# Battery Recycle Station

## スクリプト概要

このTampermonkeyユーザースクリプトは、日本小型充電式電池リサイクルセンター（JBRC）のシステムウェブサイト（`https://www.jbrc-sys.com/brsp/a2A/*`）の使いやすさを向上させるために設計されています。隠れた入力フィールドの可視化、地域ごとのリサイクルステーション検索の効率化、および検索結果の保存機能を提供します。

## 主な機能と動作

-   **隠れた要素の可視化**: 通常隠されている入力フィールドを可視化し、読み取り専用にすることで、基になるフォームデータの確認を可能にします。
-   **UIの強化**: カスタムの`div`要素（`#tampermonkey`）をピンクの背景でページに挿入し、動的に生成されるナビゲーション要素のホストとして機能させます。
-   **ローカルストレージによるデータ永続化**: ページから取得した都道府県（"todofuken"）および市区町村（"shikugun"）のデータをブラウザのローカルストレージに保存し、迅速なアクセスとフォーム生成に利用します。
-   **動的なフォーム生成**: 保存された都道府県および市区町村のデータに基づいて、インタラクティブなフォームとボタンを動的に生成・表示します。これにより、手動で選択することなく、地域ごとにリサイクルステーションを素早く検索できます。これらのフォームには、JBRC検索機能に必要な様々な隠しパラメータ（`a2sid`, `TORIATUKAI_SEIHIN`, `MEI_TODOFUKEN`, `CD_TODOFUKEN`, `MEI_KYOTEN_SIKUGUN`, `SORT_KEY`, `PAGING_OFFSET`, `DISP_MODE`）が含まれています。
-   **検索結果のスクレイピングと保存**: 検索結果のテーブルからリサイクルステーションの詳細（名前、住所、電話番号）を抽出し、ローカルストレージに保存します。これにより、セッションを跨いでデータが保持されます。
-   **スタイリングの改善**: 追加されたUI要素の視認性とレイアウトを向上させるために、基本的なCSSスタイルを適用します。

## スクリプトの目的

このスクリプトは、JBRCウェブサイト上でのバッテリーリサイクルステーションの検索プロセスを効率化し、ユーザーエクスペリエンスを向上させることを目的としています。

## 技術詳細

-   **GM_addStyle**: カスタムCSSをページに適用するために使用されます。
-   **localStorage**: 都道府県、市区町村データ、および検索結果をクライアントサイドで永続的に保存するために使用されます。
-   **DOM操作**: `document.querySelectorAll`, `document.createElement`, `appendChild`, `setAttribute`などを用いて、ページ要素の追加、変更、イベントリスナーの設定を行います。
-   **フォーム送信**: 動的に生成されたフォームを通じて、特定のURLにPOSTリクエストを送信し、検索アクションをトリガーします。

## 使用方法

1.  Tampermonkeyまたは同様のユーザースクリプトマネージャーをブラウザにインストールします。
2.  このスクリプトをインストールします。
3.  JBRCのウェブサイト（`https://www.jbrc-sys.com/brsp/a2A/*`に一致するURL）にアクセスします。
4.  ページに表示される新しいUI要素（ピンクの背景の`div`内に生成されたボタンやフォーム）を使用して、リサイクルステーションの検索と情報の保存を行います。
