# DOM Fragment Analysis (embedded-canvas-markdown-button.html)

## 1. Structure
This fragment represents a menu item within a dropdown menu, likely the one triggered by the "Download" button.

```html
<div role="menuitem" class="group __menu-item" ...>
    <div class="flex ...">
        マークダウンドキュメント（.md）
    </div>
</div>
```

## 2. Key Attributes & Classes
*   **Role**: `role="menuitem"` confirms it's part of a navigation or action menu.
*   **Classes**: `group __menu-item`.
    *   `__menu-item`: A specific class likely used by the application to style menu entries.
*   **Content**: "マークダウンドキュメント（.md）" indicates this option allows downloading the content as a Markdown file.

## 3. Context
This element appears inside the popup menu triggered by the `embedded-canvas-download-button`. It represents one of the available download formats.
