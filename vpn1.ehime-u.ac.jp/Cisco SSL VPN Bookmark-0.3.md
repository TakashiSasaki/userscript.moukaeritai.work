# Cisco SSL VPN Bookmark

## スクリプト概要

このTampermonkeyユーザースクリプトは、Cisco SSL VPNのウェブサービス（`https://*.ac.jp/+CSCOE+/portal.html`）のトップページにローカルブックマーク機能を追加します。これにより、頻繁にアクセスする内部URLを保存し、VPNのURL入力フィールドに素早く挿入できるようになります。

## 主な機能と動作

-   **ブックマークマネージャーの統合**: Cisco SSL VPNポータルページに直接、最大10個のカスタムURLブックマークを定義できるセクションを追加します。
-   **永続的なストレージ**: Tampermonkeyの`GM_setValue`と`GM_getValue`を利用して、ブックマークURLをブラウザのローカルストレージに保存および取得します。これにより、ブラウザのセッションを跨いでブックマークが保持されます。
-   **URLの高速入力**: 各ブックマークエントリには「入力」ボタンが付属しています。このボタンをクリックすると、対応する保存済みURLがメインのCisco SSL VPNのURL入力フィールド（`#unicorn_form_url`）に自動的に入力されます。
-   **視覚的なフィードバック**: スクリプトの読み込み時やブックマークURLが挿入されるたびに、メインのVPN URL入力フィールドの背景が黄色にハイライトされ、明確な視覚的フィードバックを提供します。カスタムブックマーク入力フィールドはピンク色で表示されます。
-   **ターゲットユーザー**: 特に日本の学術機関（`.ac.jp`ドメイン）がホストするCisco SSL VPNポータルにアクセスするユーザー向けに設計されています。

## スクリプトの目的

このスクリプトは、Cisco SSL VPNを介して内部リソースにアクセスするプロセスを効率化し、URLを手動で繰り返し入力したりコピー＆ペーストする手間を省くことを目的としています。

## 技術詳細

-   **Tampermonkey API**:
    -   `GM_setValue`, `GM_getValue`: ブックマークURLの永続的な保存と取得に使用されます。
-   **DOM操作**: `document.querySelector`, `document.createElement`, `appendChild`, `addEventListener`などを用いて、ページ要素の追加、変更、イベントリスナーの設定を行います。

## 使用方法

1.  Tampermonkeyまたは同様のユーザースクリプトマネージャーをブラウザにインストールします。
2.  このスクリプトをインストールします。
3.  Cisco SSL VPNのポータルページ（`https://*.ac.jp/+CSCOE+/portal.html`に一致するURL）にアクセスします。
4.  ページに新しいブックマーク入力フィールドと「入力」ボタンのセットが表示されます。
5.  各入力フィールドに頻繁に使用する内部URLを入力し、自動的に保存されます。
6.  URLをVPNの入力フィールドに適用するには、対応する「入力」ボタンをクリックします。
